server:
  port: 8084

spring:
  application:
    name: security-service

  datasource:
    url: jdbc:mysql://localhost:3306/security_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

jwt:
  secret: "THIS_IS_MY_SUPER_SECRET_KEY_AT_LEAST_32_CHARS_123456"
  expiration: 3600000

